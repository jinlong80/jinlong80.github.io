# UFW

## Command
* List
  ```bash
  sudo ufw app list
  ```

* Add
  ```bash
  sudo ufw allow 27017
  sudo ufw allow OpenSSH
  ```

* Enable
  ```bash
  sudo ufw enable
  ```

* Check Status
  ```bash
  sudo ufw status
  ``` 

